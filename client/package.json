{
  "name": "client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:desktop": "vite build --mode desktop",
    "build:mobile": "vite build --mode mobile",
    "zip": "npm run build && cd dist && zip -r ../client-prod.zip *"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.1",
    "tailwindcss": "^4.3.1",
    "vite": "^8.0.12"
  }
}
